principal {
    inteiro x = 10;

    se (x > 10) {
      escreve("maior");
    } senaose (x == 10) {
      escreve("igual");
    } senao {
      escreve("menor");
    }

    // faixa fechada [0,100]
    se ((x >= 0) e (x <= 100)) {
      escreve("entre 0 e 100");
    }
}
